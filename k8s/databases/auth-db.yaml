---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auth-db-pvc
  namespace: gymit-microservices
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-db
  namespace: gymit-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-db
  template:
    metadata:
      labels:
        app: auth-db
    spec:
      containers:
      - name: postgres
        image: postgres:16
        env:
        - name: POSTGRES_USER
          value: "authuser"
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: gymit-secrets
              key: AUTH_DB_PASSWORD
        - name: POSTGRES_DB
          value: "authdb"
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: auth-db-storage
          mountPath: /var/lib/postgresql/data
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - authuser
            - -d
            - authdb
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - authuser
            - -d
            - authdb
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: auth-db-storage
        persistentVolumeClaim:
          claimName: auth-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: auth-db
  namespace: gymit-microservices
spec:
  selector:
    app: auth-db
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP
