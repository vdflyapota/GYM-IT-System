apiVersion: v1
kind: ConfigMap
metadata:
  name: tournament-config
data:
  database_url: "sqlite:///./tournament.db"
  # For PostgreSQL in production:
  # database_url: "postgresql://user:password@postgres-service:5432/tournament_db"
