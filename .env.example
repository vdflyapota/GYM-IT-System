# Security
SECRET_KEY=please-change
JWT_SECRET_KEY=please-change-jwt

# Database (compose service hostname "db")
DATABASE_URL=postgresql+psycopg2://gymit:gymit@db:5432/gymit

# Redis
REDIS_URL=redis://redis:6379/0

# AES-256 key (base64 of 32 bytes). Generate with: openssl rand -base64 32
AES256_KEY_BASE64=REPLACE_WITH_BASE64_32BYTE_KEY

# Backups
BACKUP_BUCKET=
BACKUP_PASSPHRASE=REPLACE_WITH_STRONG_PASSPHRASE
