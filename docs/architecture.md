\# Kubernetes Deployment



The GYM IT System microservices are deployable via Kubernetes.



\- Each service is defined in `deployment.yaml`.

\- Horizontal scaling is configured with `replicas: 5`.

\- Services are exposed using `service.yaml`.

\- Ingress is configured for API Gateway access using `ingress.yaml`.



